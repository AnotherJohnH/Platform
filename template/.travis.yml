
os:
   - linux
   - osx

dist: xenial
sudo: required

addons:
  apt:
    packages:
      - libsdl2-dev

before_install:
   - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install sdl2; fi

language: cpp

script:
   - mkdir build
   - cd build
   - cmake ..
   - make

compiler:
   - clang
   - gcc
